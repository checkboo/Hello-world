# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Overview

This is a minimal "Hello World" repository. The only file is `README.md`, which contains a single HTML heading (`<h1>Hello World !!!</h1>`). There is no build system, test suite, or linting configuration.
